{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "audit:inventory": "node ./scripts/generate-dependency-inventory.mjs",
    "audit:licenses": "node ./scripts/check-third-party-licenses.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "release:validate": "pnpm run typecheck:libs && pnpm --filter @workspace/api-server run typecheck && pnpm --filter @workspace/api-server run test:unit && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/web-irc run typecheck && pnpm --filter @workspace/web-irc run test && PORT=4173 BASE_PATH=/web-irc/ pnpm --filter @workspace/web-irc run build && pnpm run audit:licenses"
  },
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.3"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
