openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: irc
    description: IRC room and message operations
paths:
  /admin/custom-roles:
    get:
      operationId: listAdminCustomRoles
      summary: List custom role definitions and grantable permissions
      responses:
        "200":
          description: Role catalog
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomRoleCatalog"
    post:
      operationId: createAdminCustomRole
      summary: Create a reusable scoped role
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomRoleInput"
      responses:
        "201":
          description: Created role
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomRole"
  /admin/custom-roles/{key}:
    parameters:
      - name: key
        in: path
        required: true
        schema: { type: string }
    patch:
      operationId: updateAdminCustomRole
      summary: Edit a custom role and its permissions
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomRoleInput"
      responses:
        "200":
          description: Updated role
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomRole"
    delete:
      operationId: retireAdminCustomRole
      summary: Retire a custom role without deleting its history
      responses:
        "200":
          description: Retired
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean }
                required: [ok]
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /irc/state:
    get:
      operationId: getIrcState
      tags: [irc]
      summary: Get current IRC room state
      description: Returns the selected channel's recent messages and current nicknames.
      parameters:
        - name: channel
          in: query
          required: true
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: IRC room state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IrcState"
        "400":
          description: Invalid channel
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /irc/join:
    post:
      operationId: joinIrcChannel
      tags: [irc]
      summary: Join an IRC channel
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IrcJoinInput"
      responses:
        "200":
          description: Joined room state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IrcState"
        "400":
          description: Invalid join request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /irc/messages:
    post:
      operationId: sendIrcMessage
      tags: [irc]
      summary: Send a message to an IRC channel
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IrcMessageInput"
      responses:
        "201":
          description: Message accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IrcMessage"
        "400":
          description: Invalid message
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /irc/events:
    get:
      operationId: streamIrcEvents
      tags: [irc]
      summary: Stream IRC room events
      description: Opens a server-sent events stream for messages and presence changes in a channel.
      parameters:
        - name: channel
          in: query
          required: true
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Server-sent events stream
          content:
            text/event-stream:
              schema:
                type: string
  /onboarding:
    get:
      operationId: getOnboardingState
      tags: [onboarding]
      summary: Get the authenticated user's onboarding state
      responses:
        "200":
          description: Onboarding state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingState"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /onboarding/{communityId}/progress:
    post:
      operationId: advanceOnboarding
      tags: [onboarding]
      summary: Advance onboarding for an owned community
      parameters:
        - name: communityId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OnboardingProgressInput"
      responses:
        "200":
          description: Updated onboarding state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingProgress"
        "403":
          description: The user cannot manage this community
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Community not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /notifications:
    get:
      operationId: listNotifications
      tags: [notifications]
      summary: List the signed-in user's notifications
      parameters:
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
        - name: archived
          in: query
          schema: { type: boolean, default: false }
      responses:
        "200":
          description: Notifications
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Notification" }
          headers:
            X-Has-More:
              description: Whether another page is available.
              schema: { type: boolean }
            X-Next-Offset:
              description: Offset to use for the next page when X-Has-More is true.
              schema: { type: integer, minimum: 0 }
  /communities/{communityId}:
    get:
      operationId: getCommunityWorkspace
      tags: [communities]
      summary: Get workspace data and bounded collection pages
      parameters:
        - name: communityId
          in: path
          required: true
          schema: { type: integer }
        - name: employeesLimit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
        - name: employeesOffset
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
        - name: invitationsLimit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
        - name: invitationsOffset
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
        - name: tasksLimit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
        - name: tasksOffset
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
        - name: channelsLimit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
        - name: channelsOffset
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
        - name: categoriesLimit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
        - name: categoriesOffset
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
        - name: assignmentsLimit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
        - name: assignmentsOffset
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
        - name: departmentsLimit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
        - name: departmentsOffset
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
        - name: locationsLimit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
        - name: locationsOffset
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
        - name: teamsLimit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
        - name: teamsOffset
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
        - name: policiesLimit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
        - name: policiesOffset
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
      responses:
        "200":
          description: Workspace data
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CommunityWorkspaceResponse" }
  /communities/{communityId}/moderation-logs:
    get:
      operationId: listModerationLogs
      tags: [moderation]
      summary: List recent moderation actions
      parameters:
        - name: communityId
          in: path
          required: true
          schema: { type: integer }
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
      responses:
        "200":
          description: Moderation actions
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ModerationAction" }
          headers:
            X-Has-More:
              description: Whether another page is available.
              schema: { type: boolean }
            X-Next-Offset:
              description: Offset to use for the next page when X-Has-More is true.
              schema: { type: integer, minimum: 0 }
  /communities/{communityId}/activity:
    get:
      operationId: listCommunityActivity
      tags: [audit]
      summary: List workspace audit activity
      parameters:
        - name: communityId
          in: path
          required: true
          schema: { type: integer }
        - name: auditLimit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
        - name: auditOffset
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
        - name: userId
          in: query
          schema: { type: string }
        - name: departmentId
          in: query
          schema: { type: integer }
        - name: locationId
          in: query
          schema: { type: integer }
        - name: action
          in: query
          schema: { type: string }
        - name: resource
          in: query
          schema: { type: string, maxLength: 120 }
        - name: from
          in: query
          schema: { type: string, format: date }
        - name: to
          in: query
          schema: { type: string, format: date }
      responses:
        "200":
          description: Workspace audit activity
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CommunityActivityResponse" }
  /communities/{communityId}/documents:
    get:
      operationId: listCommunityDocuments
      tags: [documents]
      summary: List workspace documents and bounded document pages
      parameters:
        - name: communityId
          in: path
          required: true
          schema: { type: integer }
        - name: q
          in: query
          schema: { type: string }
        - name: folderId
          in: query
          schema: { type: integer }
        - name: category
          in: query
          schema: { type: string }
        - name: documentsLimit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
        - name: documentsOffset
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
        - name: foldersLimit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
        - name: foldersOffset
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
      responses:
        "200":
          description: Workspace documents and related metadata
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DocumentListResponse" }
  /admin/overview:
    get:
      operationId: getAdminOverview
      tags: [admin]
      summary: Get administrative statistics and activity
      parameters:
        - name: activityLimit
          in: query
          schema: { type: integer, minimum: 1, maximum: 50, default: 20 }
        - name: activityOffset
          in: query
          schema: { type: integer, minimum: 0, maximum: 10000, default: 0 }
        - name: activityCursor
          in: query
          schema: { type: string, maxLength: 256 }
        - name: activityAfterCursor
          in: query
          description: Return events newer than this activity cursor.
          schema: { type: string, maxLength: 256 }
        - name: activityActor
          in: query
          schema: { type: string, maxLength: 200 }
        - name: activityAction
          in: query
          schema: { type: string, maxLength: 200 }
        - name: activityStartDate
          in: query
          description: Inclusive UTC calendar date for the earliest activity to return; must be YYYY-MM-DD.
          schema: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        - name: activityEndDate
          in: query
          description: Inclusive UTC calendar date for the latest activity to return; must be YYYY-MM-DD.
          schema: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
      responses:
        "200":
          description: Administrative overview
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminOverview" }
components:
  parameters:
    PageLimit:
      name: limit
      in: query
      description: Maximum number of records to return. Values above 100 are capped.
      schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
    PageOffset:
      name: offset
      in: query
      description: Number of records to skip.
      schema: { type: integer, minimum: 0, default: 0 }
  schemas:
    Notification:
      type: object
      required: [id, userId, type, category, body, createdAt]
      properties:
        id: { type: integer }
        userId: { type: string }
        type: { type: string }
        category: { type: string }
        body: { type: string }
        communityId: { type: integer, nullable: true }
        entityType: { type: string, nullable: true }
        entityId: { type: string, nullable: true }
        actionUrl: { type: string, nullable: true }
        readAt: { type: string, format: date-time, nullable: true }
        archivedAt: { type: string, format: date-time, nullable: true }
        deletedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
    ModerationAction:
      type: object
      required: [id, actorId, action, createdAt]
      properties:
        id: { type: integer }
        actorId: { type: string }
        targetUserId: { type: string, nullable: true }
        communityId: { type: integer, nullable: true }
        channelId: { type: integer, nullable: true }
        action: { type: string }
        details: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
    DocumentListResponse:
      type: object
      required: [folders, documents, canManage, pagination]
      properties:
        folders: { type: array, items: { type: object } }
        documents: { type: array, items: { type: object } }
        canManage: { type: boolean }
        pagination: { $ref: "#/components/schemas/PageInfo" }
        foldersPagination: { $ref: "#/components/schemas/PageInfo" }
    CommunityWorkspaceResponse:
      type: object
      required: [community, pagination]
      properties:
        community: { type: object }
        members: { type: array, items: { type: object } }
        channels: { type: array, items: { type: object } }
        categories: { type: array, items: { type: object } }
        assignments: { type: array, items: { type: object } }
        departments: { type: array, items: { type: object } }
        locations: { type: array, items: { type: object } }
        teams: { type: array, items: { type: object } }
        policies: { type: array, items: { type: object } }
        employees: { type: array, items: { type: object } }
        invitations: { type: array, items: { type: object } }
        tasks: { type: array, items: { type: object } }
        pagination:
          type: object
          additionalProperties: { $ref: "#/components/schemas/PageInfo" }
    PageInfo:
      type: object
      required: [limit, offset, hasMore]
      properties:
        limit: { type: integer, minimum: 1, maximum: 100 }
        offset: { type: integer, minimum: 0 }
        hasMore: { type: boolean }
    AdminOverview:
      type: object
      required: [stats, users, channels, categories, recentMessages, activity, activityPagination]
      properties:
        stats: { type: object }
        users: { type: array, items: { type: object } }
        channels: { type: array, items: { type: object } }
        categories: { type: array, items: { type: object } }
        recentMessages: { type: array, items: { type: object } }
        activity: { type: array, items: { type: object } }
        activityPagination: { $ref: "#/components/schemas/AdminActivityPagination" }
    AdminActivityPagination:
      type: object
      required: [limit, offset, hasMore, nextOffset, nextCursor, newestCursor, newerHasMore]
      properties:
        limit: { type: integer, minimum: 1, maximum: 50 }
        offset: { type: integer, minimum: 0 }
        hasMore: { type: boolean, description: Whether older events are available. }
        nextOffset: { type: ["integer", "null"], minimum: 0 }
        nextCursor: { type: ["string", "null"] }
        newestCursor: { type: ["string", "null"], description: Cursor for the newest returned event. }
        newerHasMore: { type: boolean, description: Whether more newer events remain after an activityAfterCursor request. }
    AuditEntry:
      type: object
      required: [id, action, createdAt]
      properties:
        id: { type: integer }
        actorId: { type: string, nullable: true }
        actor: { type: string }
        action: { type: string }
        departmentId: { type: integer, nullable: true }
        locationId: { type: integer, nullable: true }
        resourceType: { type: string, nullable: true }
        resourceId: { type: string, nullable: true }
        targetLabel: { type: string, nullable: true }
        details: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
    CommunityActivityResponse:
      type: object
      required: [entries, actions, pagination]
      properties:
        entries: { type: array, items: { $ref: "#/components/schemas/AuditEntry" } }
        actions: { type: array, items: { type: string } }
        pagination: { $ref: "#/components/schemas/PageInfo" }
    CustomRoleInput:
      type: object
      required: [label, description, scopeType, permissions]
      properties:
        label: { type: string, minLength: 1, maxLength: 60 }
        description: { type: string, maxLength: 240 }
        scopeType: { type: string, enum: [community, category, department, channel] }
        permissions:
          type: array
          minItems: 1
          items: { type: string }
    CustomRole:
      type: object
      required: [key, label, description, scopeType, isActive, permissions, createdBy, createdAt, updatedAt]
      properties:
        key: { type: string }
        label: { type: string }
        description: { type: string }
        scopeType: { type: string, enum: [community, category, department, channel] }
        isActive: { type: boolean }
        permissions:
          type: array
          items: { type: string }
        createdBy: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CustomRoleCatalog:
      type: object
      required: [roles, permissions]
      properties:
        roles:
          type: array
          items:
            $ref: "#/components/schemas/CustomRole"
        permissions:
          type: array
          items:
            type: object
            required: [key, description]
            properties:
              key: { type: string }
              description: { type: string }
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
    IrcMessage:
      type: object
      properties:
        id:
          type: string
        channel:
          type: string
        nick:
          type: string
        text:
          type: string
        timestamp:
          type: string
          format: date-time
        kind:
          type: string
          enum: [message, system]
      required: [id, channel, nick, text, timestamp, kind]
    IrcUser:
      type: object
      properties:
        nick:
          type: string
        status:
          type: string
          enum: [online, away]
        color:
          type: string
      required: [nick, status, color]
    IrcState:
      type: object
      properties:
        channel:
          type: string
        topic:
          type: string
        messages:
          type: array
          items:
            $ref: "#/components/schemas/IrcMessage"
        users:
          type: array
          items:
            $ref: "#/components/schemas/IrcUser"
      required: [channel, topic, messages, users]
    IrcJoinInput:
      type: object
      properties:
        channel:
          type: string
          minLength: 1
        nick:
          type: string
          minLength: 1
          maxLength: 24
      required: [channel, nick]
    IrcMessageInput:
      type: object
      properties:
        channel:
          type: string
          minLength: 1
        nick:
          type: string
          minLength: 1
          maxLength: 24
        text:
          type: string
          minLength: 1
          maxLength: 500
      required: [channel, nick, text]
    OnboardingCommunity:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        slug:
          type: string
        plan:
          type: string
          enum: [free_community, paid_workspace]
        onboardingStep:
          type: integer
        joined:
          type: boolean
        canManage:
          type: boolean
      required: [id, name, slug, plan, onboardingStep, joined, canManage]
    OnboardingState:
      type: object
      properties:
        nextStep:
          type: string
          enum: [create, configure, invite, start]
        ownerCommunity:
          oneOf:
            - $ref: "#/components/schemas/OnboardingCommunity"
            - type: "null"
        communities:
          type: array
          items:
            $ref: "#/components/schemas/OnboardingCommunity"
      required: [nextStep, ownerCommunity, communities]
    OnboardingProgressInput:
      type: object
      properties:
        step:
          type: integer
          enum: [2, 9]
      required: [step]
    OnboardingProgress:
      type: object
      properties:
        community:
          $ref: "#/components/schemas/OnboardingCommunity"
        nextStep:
          type: string
          enum: [create, configure, invite, start]
      required: [community, nextStep]
